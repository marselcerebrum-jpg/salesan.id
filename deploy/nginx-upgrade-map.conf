# Lets one location serve both WebSocket and plain HTTP: "upgrade" only when
# the client asked for it, otherwise the header is dropped.
#   cp deploy/nginx-upgrade-map.conf /etc/nginx/conf.d/upgrade-map.conf
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
